name: Flatpak

# Builds the Flatpak in the Flathub builder container to validate the manifest before
# submitting to Flathub. Runs on the flathub branch and on demand.
on:
  push:
    branches: [flathub]
    paths:
      - "packaging/flatpak/**"
      - ".github/workflows/flatpak.yml"
  workflow_dispatch:

jobs:
  flatpak:
    name: Build (x86_64)
    runs-on: ubuntu-latest
    container:
      image: ghcr.io/flathub-infra/flatpak-github-actions:gnome-47
      options: --privileged
    steps:
      - uses: actions/checkout@v4

      - name: Generate offline Python dependency sources
        working-directory: packaging/flatpak
        run: |
          set -euo pipefail
          python3 -m pip install --quiet requirements-parser aiohttp toml
          # The repo's `flatpak-pip-generator` is a symlink; fetch the real .py.
          curl -fL -o fpg.py https://raw.githubusercontent.com/flatpak/flatpak-builder-tools/master/pip/flatpak-pip-generator.py
          # Binary-wheel packages must use platform wheels (no usable sdist build);
          # pygobject/pycairo stay as sdists built against the runtime. Both arches.
          # lxml/pygobject/pycairo ship in the GNOME runtime (read-only), so don't vendor
          # them — pip uses the runtime copies at build time.
          python3 fpg.py \
            --runtime=org.gnome.Sdk//47 \
            --requirements-file=requirements.txt \
            --prefer-wheels=numpy,opencv-python-headless,pillow,pikepdf,pypdfium2 \
            --ignore-installed=lxml,pygobject,pycairo \
            --output=python3-deps
          rm -f fpg.py
          echo "=== generated python3-deps.json (first module) ==="
          head -n 30 python3-deps.json

      - uses: flatpak/flatpak-github-actions/flatpak-builder@v6
        with:
          bundle: auto-rotate.flatpak
          manifest-path: packaging/flatpak/io.github.gedejong.auto_rotate.yaml
          cache-key: flatpak-builder-${{ github.sha }}
          upload-artifact: true
